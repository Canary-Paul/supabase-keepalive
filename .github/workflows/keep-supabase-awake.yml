name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 0 */2 * *" # every 2 days (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST
        run: |
          curl -s https://ngvbdnhwvdteipdaggoe.supabase.co/rest/v1/ || true


